import React from 'react';
import "./Tarefa.css";

// Componente 'Tarefa' - é o texto dentro de 'Tarefas'

const Tarefa = ({ tarefa }) => {
   // return <h1>{tarefa.title}</h1> // vai pegar pelo título do objeto
   return ( <h1>tarefa</h1>)
};
 
export default Tarefa ;